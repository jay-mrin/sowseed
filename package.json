{
  "name": "sow-your-seed",
  "version": "1.0.0",
  "private": true,
  "description": "Personalised digital writing custom-order platform with Supabase payment backends.",
  "scripts": {
    "dev": "python3 -m http.server 5173",
    "check:js": "node --check src/app.js",
    "check:repo": "node --test tests/repository-integrity.test.mjs",
    "lint": "eslint src/*.js tests/*.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run check:js && npm run lint && npm test",
    "supabase:functions:serve": "supabase functions serve --env-file .env",
    "supabase:db:push": "supabase db push",
    "supabase:deploy": "supabase functions deploy public-bootstrap create-paypal-order capture-paypal-order paypal-webhook admin-profile admin-settings admin-posts admin-donations admin-analytics track-checkout-event post-engagement"
  },
  "devDependencies": {
    "eslint": "^9.39.5",
    "supabase": "^2.39.2"
  }
}
